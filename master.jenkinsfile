@Library('github.com/sivakumarvaddi786/seed@main') _
pipeline {
    agent any
    
    stages {
        stage('Create Folders and Jobs') {
            steps {
                script {
                    // Ensure that file operations are performed within a context
                        // Call your library function here
                        sh 'ls -l'
                        createFoldersAndJobs("jobs.dsl")
                }
            }
        }
    }
}
